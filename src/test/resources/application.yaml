spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: test-secret-key-for-testing-purposes-only
  issuer: test-issuer
  expire-hours: 1
  admin-expire-hours: 2
